package kr.remerge.stylehub.global.exception;

import lombok.Getter;
import lombok.RequiredArgsConstructor;

// 서비스 전체에서 사용하는 에러 코드 모음
// HTTP 상태코드 + 에러 메시지를 한 곳에서 관리
@Getter
@RequiredArgsConstructor
public enum ErrorCode {

    // ───────────────────────────────────────────
    // 인증 / 인가
    // ───────────────────────────────────────────
    UNAUTHORIZED(401, "로그인이 필요합니다."),
    FORBIDDEN(403, "접근 권한이 없습니다."),
    INVALID_TOKEN(401, "유효하지 않은 토큰입니다."),
    EXPIRED_ACCESS_TOKEN(401, "액세스 토큰이 만료되었습니다."),
    REFRESH_TOKEN_EXPIRED(401, "리프레시 토큰이 만료되었습니다."),

    // ───────────────────────────────────────────
    // 유저
    // ───────────────────────────────────────────
    USER_NOT_FOUND(404, "유저를 찾을 수 없습니다."),
    INVALID_PASSWORD(401, "비밀번호가 올바르지 않습니다."),
    LOGIN_ATTEMPTS_EXCEEDED(401, "로그인 시도 횟수를 초과했습니다."),
    DUPLICATE_EMAIL(409, "이미 사용 중인 이메일입니다."),
    USER_PENDING(403, "승인 대기 중인 계정입니다."),
    USER_SUSPENDED(403, "정지된 계정입니다."),
    USER_DELETED(403, "탈퇴한 계정입니다."),

    // ───────────────────────────────────────────
    // 회사
    // ───────────────────────────────────────────
    COMPANY_NOT_FOUND(404, "회사를 찾을 수 없습니다."),
    DUPLICATE_BUSINESS_NUMBER(409, "이미 등록된 사업자등록번호입니다."),

    // ───────────────────────────────────────────
    // 상품
    // ───────────────────────────────────────────
    PRODUCT_NOT_FOUND(404, "상품을 찾을 수 없습니다."),
    PRODUCT_OPTION_NOT_FOUND(404, "상품 옵션을 찾을 수 없습니다."),
    OUT_OF_STOCK(400, "재고가 부족합니다."),

    // ───────────────────────────────────────────
    // 주문
    // ───────────────────────────────────────────
    ORDER_NOT_FOUND(404, "주문을 찾을 수 없습니다."),
    INVALID_ORDER_STATUS(400, "유효하지 않은 주문 상태입니다."),
    ORDER_CANCEL_NOT_ALLOWED(400, "취소할 수 없는 주문입니다."),

    // ───────────────────────────────────────────
    // 견적
    // ───────────────────────────────────────────
    QUOTE_NOT_FOUND(404, "견적서를 찾을 수 없습니다."),
    QUOTE_EXPIRED(400, "만료된 견적서입니다."),

    // ───────────────────────────────────────────
    // 계약
    // ───────────────────────────────────────────
    CONTRACT_NOT_FOUND(404, "계약서를 찾을 수 없습니다."),
    CONTRACT_ALREADY_SIGNED(400, "이미 서명된 계약서입니다."),

    // ───────────────────────────────────────────
    // 소싱
    // ───────────────────────────────────────────
    SOURCING_NOT_FOUND(404, "소싱 요청을 찾을 수 없습니다."),

    // ───────────────────────────────────────────
    // 카테고리
    // ───────────────────────────────────────────
    CATEGORY_NOT_FOUND(404, "카테고리를 찾을 수 없습니다."),
    INVALID_CATEGORY_COUNT(400, "선호 카테고리는 3개에서 5개 사이여야 합니다."),

    // ───────────────────────────────────────────
    // 공통
    // ───────────────────────────────────────────
    INVALID_INPUT(400, "잘못된 입력값입니다."),
    INTERNAL_SERVER_ERROR(500, "서버 오류가 발생했습니다.");

    private final int httpStatus;  // HTTP 상태 코드
    private final String message;  // 에러 메시지
}
